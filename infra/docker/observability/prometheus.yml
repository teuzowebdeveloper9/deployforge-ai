global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090

  # Add service /metrics targets after each service exposes Prometheus-format metrics.
